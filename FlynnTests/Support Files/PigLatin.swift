//
//  FlynnTests.swift
//  FlynnTests
//
//  Created by Rocco Bowling on 5/10/20.
//  Copyright © 2020 Rocco Bowling. All rights reserved.
//

import XCTest

@testable import Flynn
/*
protocol Flowable {
    func target(target:Actor, arg:Any)
}

class PigLatin: Actor & Flowable {
    lazy var translate = Behavior(self) { (args:BehaviorArgs) in
        // takes a string, converts it to pig latin, passes it on to the next actor
        let s:String = args[0]
        let target:Actor = args[1]
    }
    
    func flow(target:Actor, arg:Any) {
        
    }
}

class WordCounter: Actor {
    var word_count:Int = 0
    lazy var count = Behavior(self) { (args:BehaviorArgs) in
        // takes a string, converts it to pig latin, passes it on to the next actor
        let s:String = args[0]
        self.word_count += s.split(separator: " ").count
    }
}
*/
